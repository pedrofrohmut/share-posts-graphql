import React from "react"

const HomePage: React.FC = () => (
  <div className="container">
    <div className="jumbotron jumbotron-fluid p-4 mt-3">
      <div className="container">
        <h1 className="display-3">SharePosts</h1>
        <p className="lead">
          Simple social network built on the ReactTS and GraphQL user interface
          and with a C#, AspNetCore, EntityFrameworkCore and GraphQL backend
        </p>
      </div>
    </div>
  </div>
)

export default HomePage
